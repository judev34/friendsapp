<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Événement créé avec succès</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #007bff;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 5px 5px 0 0;
        }
        .content {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 0 0 5px 5px;
        }
        .event-details {
            background-color: white;
            padding: 15px;
            border-radius: 5px;
            margin: 15px 0;
        }
        .button {
            display: inline-block;
            background-color: #28a745;
            color: white;
            padding: 12px 24px;
            text-decoration: none;
            border-radius: 5px;
            margin: 10px 0;
        }
        .footer {
            text-align: center;
            color: #666;
            font-size: 12px;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>✅ Événement créé avec succès</h1>
    </div>
    
    <div class="content">
        <p>Bonjour {{ organizer.firstName }},</p>
        
        <p>Votre événement a été créé avec succès sur notre plateforme !</p>
        
        <div class="event-details">
            <h3>{{ event.title }}</h3>
            <p><strong>📅 Date :</strong> {{ event.startDate|date('d/m/Y à H:i') }}</p>
            <p><strong>📍 Lieu :</strong> {{ event.location }}</p>
            {% if event.maxParticipants %}
                <p><strong>👥 Participants max :</strong> {{ event.maxParticipants }}</p>
            {% endif %}
            {% if event.price and event.price != '0.00' %}
                <p><strong>💰 Prix :</strong> {{ event.price }}€</p>
            {% else %}
                <p><strong>💰 Prix :</strong> Gratuit</p>
            {% endif %}
        </div>
        
        <p><strong>Prochaines étapes :</strong></p>
        <ul>
            <li>Votre événement est actuellement en brouillon</li>
            <li>Vérifiez tous les détails</li>
            <li>Publiez votre événement pour que les participants puissent s'inscrire</li>
        </ul>
        
        <p>
            <a href="#" class="button">Gérer mon événement</a>
        </p>
        
        <p>Merci d'utiliser notre plateforme !</p>
        
        <p>L'équipe EventApp</p>
    </div>
    
    <div class="footer">
        <p>Cet email a été envoyé automatiquement, merci de ne pas y répondre.</p>
    </div>
</body>
</html>
