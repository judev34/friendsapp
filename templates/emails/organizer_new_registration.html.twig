<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Nouvelle inscription</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #6f42c1;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 5px 5px 0 0;
        }
        .content {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 0 0 5px 5px;
        }
        .event-details {
            background-color: white;
            padding: 15px;
            border-radius: 5px;
            margin: 15px 0;
        }
        .participant-info {
            background-color: #e8f4fd;
            padding: 15px;
            border-radius: 5px;
            margin: 15px 0;
        }
        .button {
            display: inline-block;
            background-color: #6f42c1;
            color: white;
            padding: 12px 24px;
            text-decoration: none;
            border-radius: 5px;
            margin: 10px 0;
        }
        .footer {
            text-align: center;
            color: #666;
            font-size: 12px;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>🎉 Nouvelle inscription !</h1>
    </div>
    
    <div class="content">
        <p>Bonjour {{ organizer.firstName }},</p>
        
        <p>Bonne nouvelle ! Une nouvelle personne s'est inscrite à votre événement :</p>
        
        <div class="event-details">
            <h3>{{ event.title }}</h3>
            <p><strong>📅 Date :</strong> {{ event.startDate|date('d/m/Y à H:i') }}</p>
            <p><strong>📍 Lieu :</strong> {{ event.location }}</p>
        </div>
        
        <div class="participant-info">
            <h4>👤 Nouveau participant</h4>
            <p><strong>Nom :</strong> {{ registration.user.fullName }}</p>
            <p><strong>Email :</strong> {{ registration.user.email }}</p>
            <p><strong>Date d'inscription :</strong> {{ registration.registeredAt|date('d/m/Y à H:i') }}</p>
            <p><strong>Statut :</strong> {{ registration.status.label }}</p>
        </div>
        
        {% set stats = event.confirmedRegistrationsCount %}
        <p><strong>Statistiques actuelles :</strong></p>
        <ul>
            <li>Inscriptions confirmées : {{ stats }}</li>
            {% if event.maxParticipants %}
                <li>Places restantes : {{ event.availableSpots }}</li>
                <li>Taux de remplissage : {{ ((stats / event.maxParticipants) * 100)|round }}%</li>
            {% endif %}
        </ul>
        
        <p>
            <a href="#" class="button">Gérer les inscriptions</a>
            <a href="#" class="button">Voir l'événement</a>
        </p>
        
        <p>Félicitations pour le succès de votre événement !</p>
        
        <p>L'équipe EventApp</p>
    </div>
    
    <div class="footer">
        <p>Cet email a été envoyé automatiquement, merci de ne pas y répondre.</p>
    </div>
</body>
</html>
