<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Événement publié</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #28a745;
            color: white;
            padding: 20px;
            text-align: center;
            border-radius: 5px 5px 0 0;
        }
        .content {
            background-color: #f8f9fa;
            padding: 20px;
            border-radius: 0 0 5px 5px;
        }
        .event-details {
            background-color: white;
            padding: 15px;
            border-radius: 5px;
            margin: 15px 0;
        }
        .button {
            display: inline-block;
            background-color: #007bff;
            color: white;
            padding: 12px 24px;
            text-decoration: none;
            border-radius: 5px;
            margin: 10px 0;
        }
        .footer {
            text-align: center;
            color: #666;
            font-size: 12px;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>🎉 Événement publié !</h1>
    </div>
    
    <div class="content">
        <p>Bonjour {{ organizer.firstName }},</p>
        
        <p>Excellente nouvelle ! Votre événement est maintenant <strong>publié</strong> et visible par tous les utilisateurs.</p>
        
        <div class="event-details">
            <h3>{{ event.title }}</h3>
            <p><strong>📅 Date :</strong> {{ event.startDate|date('d/m/Y à H:i') }}</p>
            <p><strong>📍 Lieu :</strong> {{ event.location }}</p>
            <p><strong>📝 Description :</strong> {{ event.description|slice(0, 100) }}{% if event.description|length > 100 %}...{% endif %}</p>
        </div>
        
        <p><strong>Que se passe-t-il maintenant ?</strong></p>
        <ul>
            <li>Les participants peuvent désormais s'inscrire à votre événement</li>
            <li>Vous recevrez des notifications pour chaque nouvelle inscription</li>
            <li>Vous pouvez suivre les inscriptions depuis votre tableau de bord</li>
        </ul>
        
        <p>
            <a href="#" class="button">Voir mon événement</a>
            <a href="#" class="button">Gérer les inscriptions</a>
        </p>
        
        <p>Bonne chance pour votre événement !</p>
        
        <p>L'équipe EventApp</p>
    </div>
    
    <div class="footer">
        <p>Cet email a été envoyé automatiquement, merci de ne pas y répondre.</p>
    </div>
</body>
</html>
